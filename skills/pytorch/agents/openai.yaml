interface:
  display_name: "PyTorch"
  short_description: "Modern, reliable single-machine PyTorch runtime, training loop, and performance guidance"
  default_prompt: "Use the PyTorch skill when the request matches its SKILL.md description."
