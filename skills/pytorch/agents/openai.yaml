interface:
  display_name: "PyTorch"
  short_description: "Modern, reliable single-machine PyTorch runtime, training loop, and performance guidance"
  default_prompt: "Use $pytorch for single-machine PyTorch tasks (CPU, single-GPU, and single-machine multi-GPU): confirm the runtime stack first, keep the main code paths readable, and prefer modern, native, reliable PyTorch patterns for modules/data/loops/devices+dtypes/AMP/state_dict checkpoints/torch.compile/perf. Do not install or upgrade packages without explicit approval."
