interface:
  display_name: "AI4Science"
  short_description: "Stage-aware AI experiment guidance for minimal structure, representation, logging, and artifacts"
  default_prompt: "Use the AI4Science skill when the request matches its SKILL.md description."
